# P56 wave visual findings inventory (P56-Z wave-close deliverable)

| Field | Value |
|---|---|
| Written | 2026-07-14, P56-Z (closer) |
| Mandate | Owner ruling (briefing log 2026-07-13): grade EVERY P56 surface against directive 1a with an honest pass/fail of the RENDERED RESULT, not the sessions' paperwork. Compliance artifacts (teardowns, evidence files) are not proof of design effort |
| Consumer | The dedicated design-ONLY wave (doc 19) is briefed directly from this inventory |
| Rules honored | No P56 session redid, iterated, or redesigned any deferred item this wave (owner amendment); GATE-05 and the wave close do NOT block on these items; functional/data/a11y/perf gates ran at full strength separately |
| Method | Fresh FIX-39 capture set (490 images, both themes, 7 widths, 2026-07-14), the closer's own review of the rendered captures, the wave-close pre-delivery + mobile audits (Opus, side-by-side vs the sessions' reference captures), and the sessions' evidence folders for member-surface renders |

Grading bar (directive 1a, both halves required): (1) clear, orderly, logical, genuinely useful; (2) breathtaking / award-grade ("Maserati and Lamborghini of visuals"). PASS below means "meets both halves at state-of-the-art level"; PARTIAL means professionally executed but short of the breathtaking half; FAIL means reads generic/AI-default as rendered.

## 1. OWNER-DEFERRED FAILURES (recorded verbatim; transferred to the design wave; DO NOT redo this wave)

### 1.1 Unicorn celebration embed: REJECTED AS DESIGNED (owner ruling 2, stands unchanged)
- Component: `components/progress/training/celebration-scene.tsx` (P56-B, /progress/training records-and-milestones hero).
- The owner's words: whoever put the unicorn animation in did NOT actually use unicorn.studio to DESIGN; they threw a raw surveyed scene in half-assed with no thought to how it melds/integrates with the app. The survey's scene list was raw candidate material, never finished design.
- As built: raw surveyed scene id `gzVuIBHLlxrggwcxBuXF` ("Huly" laser) embedded untouched; no editor composition, no token/theme matching (neither theme), no runtime variables bound to member data.
- KEPT (correct per the ruling): the loading discipline: IntersectionObserver moment-of-use load, on-demand SDK script, zero npm deps, zero first-load JS, prefers-reduced-motion = scene never loads with the designed static gold-wash standing alone, no watermark (Legend), no sound/haptics.
- Required for ANY future unicorn usage (this surface and every other): compose in the editor on the owner's account: colors/lighting matched to app tokens in BOTH themes, elements integrated with the surface, runtime variables BOUND to real member data, then export the designed scene. If editor automation cannot achieve it, escalate to the owner; never ship the raw scene.
- Verdict: OWNER-DEFERRED FAIL (design wave owns the redo).

### 1.2 "Weird row of squares" (owner flag; probable element identified; pending owner confirmation)
- The owner flagged "a weird row of squares"; the coordinator's correction (briefing log 2026-07-13) identifies the probable suspect as the `WeekBars` / `MiniBars` row treatment in `components/panels/visuals.tsx`.
- Rendered-result confirmation by the closer (fresh captures, dark 1440): in EMPTY states the treatment literally renders a row of uniform full-height dashed hollow squares:
  - `WeekBars` (visuals.tsx:106): unlogged days render height=100% dashed hollow slots; a week with no logs = 7 identical outlined squares.
  - `MiniBars hollow` (visuals.tsx:147): `nutrition-panel.tsx:217` renders `values={[1,1,1,1,1,1,1]}` hollow = exactly seven identical dashed squares (the single strongest match for the owner's words, visible on the first-run and lapsed personas of `/dev/fixtures/today-panels`).
- Where the treatment appears (all P56 surfaces consuming it): `components/today/hydration-panel.tsx`, `nutrition-panel.tsx` (empty week row), `status-strip.tsx` (MiniBars + WeekBars cells), `up-next-panel.tsx` (sleep bars), `progress-highlights.tsx` (2 uses), `components/progress/training/training-analytics-view.tsx` (7-day strip), plus the P2 demo pages.
- Populated states read materially better (varying heights, domain fills); the failure concentrates in the unlogged/empty renders where every slot is a full-height dashed box.
- Constraint on the fix: the data-state law (missing is never drawn as zero) is WHY unlogged days are hollow-not-zero; the design wave must redesign the honest-absence treatment, not remove honesty.
- Verdict: OWNER-FLAGGED FAIL (pending the owner's confirmation of the element; design wave owns the redesign).

### 1.3 Calendar heatmap: NOT banned; awaits the owner's own review
- `components/charts/calendar-heatmap.tsx` (A's primitive; consumed by A's overview Nutrition + Consistency sections and B's training consistency calendar).
- Ruling 1 was WITHDRAWN by the owner correction: the multi-week heatmap grid is NOT the "square garbage" element; it goes to the OWNER FOR REVIEW with screenshots before any verdict (it may stay if genuinely top-notch).
- State as rendered (fresh captures): B's P1 cell-size cap fix landed (cells capped ~16px, contained everywhere, re-verified 130px); the coordinator's earlier pastel finding drove a ramp fix request that was then DEFERRED by the owner amendment; the alpha-faded ramp concern therefore remains OPEN as recorded, unremediated by design this wave.
- Screenshots for the owner's review: `artifacts/fixture-screenshots/progress-overview-consistent/` (Nutrition + Consistency sections) and `training/` (consistency calendar), both themes; closer's read: legible and GitHub-familiar at 1440 dark; low-intensity steps still read faded per the coordinator's original finding.
- Verdict: OWNER REVIEW PENDING (no session verdict permitted).

## 2. Per-surface directive-1a grades (rendered result)

Vocabulary: **PASS** = state-of-the-art AND distinctive (both 1a halves); **PARTIAL** = professionally excellent, matches the best references, but does not exceed them into "breathtaking"; **FAIL** = reads generic/AI-default as rendered, or carries an owner-flagged element. Grades are the closer's own review of the fresh captures plus the wave-close Opus audits' side-by-side verdicts (rendered result only; session paperwork ignored).

| Surface (owner) | Clarity/logic half | Breathtaking half | Grade | Rendered-result notes |
|---|---|---|---|---|
| /today shell: header + status strip + Up next + consistency (P56-D) | Strong: day readable in 10s (fresh ACC-01 evidence), words+number in every cell, whole-cell targets | Good: goal-met emerald rings, perfect-week glow capsule (`--shadow-glow-positive`), tokenized reward moments; composition MATCHes the WHOOP/MacroFactor/Oura references | **PARTIAL** | The strongest shell the app has had; matches the best references but does not exceed them. The consistency matrix on full-saturation domain tokens escapes the pastel class deliberately |
| /today tracking panels: Nutrition / Hydration / Sleep, POPULATED states (P56-C) | Strong: one primary action, honest coverage lines, per-day target grading | Signature visuals real: CalorieArc remaining-first gauge, LiquidGauge vessel, NightColumns moonlit week; NumberTicker headline | **PARTIAL** | Populated states are pro-grade and distinct per domain. See §1.2 for the EMPTY states, which carry the owner-flagged square rows |
| /today tracking panels, EMPTY/first-run states (P56-C on the shared P2 treatment) | Honest (designed words + one CTA, compact heights, fresh ACC-09 evidence) | Fails: rows of uniform dashed hollow squares (see §1.2) | **FAIL (owner-flagged)** | The single most owner-visible miss; transfers to the design wave |
| /progress overview, nine sections (P56-A) | Strong: nine sections, explicit empty/locked, registered metrics, goal outcomes | Mixed: emerald body trend with glow + ring gauges are strong; the Training section's saturated solid-red weekly bars read as an alarm state at a glance (A escalated: keep-for-color-law vs re-tone = owner call); heatmaps pending owner review | **PARTIAL** | The red-bars adjudication and the heatmap review are the two owner calls that decide this surface's final grade |
| /progress/body (P56-A) | Strong: full Body capability set preserved (24/24), migrated chart with URL state | Good: trend glow, verdict language, designed states | **PARTIAL** | Light-theme emerald reward text measured below AA here (see §3, owner adjudication) |
| /progress/training (P56-B) | Strong: canonical, source-linked end to end (fresh GATE-05 click-through), one range control | Best surface of the wave: audit graded **MATCH+ vs Hevy/Strong**; gold PR dots, adherence rings, muscle-focus bars, milestone timeline | **PARTIAL overall / FAIL element** | The celebration hero's unicorn embed is the owner-REJECTED raw scene (§1.1); the static gold-wash reduced-motion treatment, ironically, is the better-integrated render today |
| Plans + Goals summaries + Progress highlights on /today (P56-E) | Strong: summaries reconcile with detail (one-canonical-value verified live, incl. the two P1 fixes at close), SegmentStrip progression, honest reward states ("Reached", "Planned meals covered") | Adequate-plus: clean domain toning, but the highlight tiles read utilitarian next to the panels' signature visuals; E deferred the Milestone tile + vs-prior-week deltas | **PARTIAL** | E's own design-wave notes (eyebrow/chevron hierarchy) stand |
| /plans index (P56-E) | Strong: full management capability relocated intact; named-confirm delete landed at close | Sparse at desktop: audit filed dead-space P3 at 1440 | **PARTIAL** | Composition polish queued (§3) |
| /account/appearance (P56-D) | Strong: full customizer relocated, usable at all phone widths (fresh audit PASS) | Sparse at 1440 (D's own P3-4) | **PARTIAL** | Same class as /plans: functional relocation first, composition polish queued |

Wave-level honest summary for the owner: NOTHING in this wave ships generic-default, and the two strongest surfaces (training analytics, Today shell) sit at parity with the best category references. But by the owner's own 1a bar (exceed the references into award-grade), the wave lands at STRONG PARITY, not yet breathtaking, and the two owner-flagged elements (raw unicorn scene, square rows) plus the owner-pending heatmap/red-bars calls are exactly where the dedicated design wave should spend its ambition.

## 3. Cross-cutting rendered-result notes for the design wave

1. **AA adjudications for the owner (measured at close, both themes; mobile-audit.md):**
   - Future-day week-strip labels: 2.06:1 dark / 2.01:1 light (13-17px). Fails 4.5:1 if treated as text; the WCAG inactive-component exemption is arguable (the cells are non-interactive future placeholders whose letters reappear at passing contrast once active) but the letters are the only day identifier. Closer's recommendation: treat as inactive-exempt for this release AND raise the de-emphasis token toward ~/70 in the design wave so the question disappears.
   - Light-theme emerald reward text: "-6.1 lb" / "-1.9 lb/wk" 2.47:1, measurement deltas 2.37:1, "Trending toward your goal" banner ~2.2:1 on /progress/body and /goals/[id] (dark passes). In tension with the owner's reward-glow order; needs a darker light-theme `positive-text` token, which is a design-wave token decision, not a close-time restyle.
   - Neutral DeltaTag pairing 4.40:1 light (marginal), muted caption suffixes ~2.9:1 light: same muted-token family, fold into the same token pass.
2. **Red-on-positive semantic collision** (pre-delivery audit + P56-A's escalation + P56-D's teardown note): the blood domain accent on training bars reads as alarm/error to a fresh eye. One owner decision (keep the color law as-is vs give training a non-alarm accent) resolves it across /progress, /progress/training, and /workouts at once.
3. **DEC-10 (open owner decision):** populated quick-log panel heights vs the owner-mandated full anatomy; raise the budget (~360 desktop) or trim anatomy. Never silently trim.
4. **Sub-44px control heights, MOB-sweep class** (queued in MTL alongside the app-wide 34px dialog close X): panel range controls 34px tall on /progress/training + /hydration + /sleep; secondary actions 34-38px ("View the workout", "Show all 55", "Start a workout", "View full plan"). The /progress/body range-segment WIDTHS 37-43px remain the separately-deferred polish item.
5. **Session-queued design-wave notes, consolidated:** highlight-tile eyebrow/chevron hierarchy (E); PlanEditor centered modal vs bottom sheet (E, MOB-20 overlap); stale goal outcomes undated (E); /sleep page scaffolding parity with /hydration (C); first-run panels hide the footer so goal editing is unreachable before the first log (C; owner call whether designed-empty gains a second quiet action); desktop dead space on /plans + /account/appearance (close audit); /progress records-count label mismatch ("52 in 30 days · 110 all time" vs "Show all 55"; close audit P3); quick-add popover vs thumb-zone bottom sheet on /progress/body (A); single-goal grid gutter (A); Basic-tier Goals-unlocked asymmetry on the overview (A, owner intent check); volume y-axis -190 floor tick (B); /workouts/history month groups reading the capped 200-slice (B); Fitbod-style normalized strength index as a new-metric owner decision (B).
6. **Celebration/reward inventory for the design wave:** the wave's reward moments as rendered: emerald goal-met rings + perfect-week glow capsule (shell), covered/reached states (E), gold PR dots + record rows + milestone timeline + gold-wash hero (B), first-record celebration band (B). The unicorn redo (§1.1) should design ONE coherent celebration language across these, in the editor, tokens-bound, both themes.
